# Session 20260418-201947 Plan

## 目标
修复节点22（LoRA）评审 5/10 的三个问题，让演示数据真正支撑教学声明。

## 方案

### 问题1 [CRITICAL]：目标矩阵改低秩
- 文件：`tools/gen_nb_22.py` Cell 5（约121行）
- 改：`W_target_delta = np.random.randn(d_out, d_in) * 0.3`
- 为：`r_true = 4; W_target_delta = (np.random.randn(d_out, r_true) @ np.random.randn(r_true, d_in)) * 0.3`
- 同步删掉错误注释 `(接近0 = 效果相当)` → 改为 `(LoRA拟合低秩目标，效果应接近全量微调)`

### 问题2：RuntimeWarning 处理
- 文件：`tools/gen_nb_22.py` Cell 3（约75行）
- 在 Cell 3 的 import 区域加 `np.seterr(all='ignore')` + 注释说明原因
- 或改写演示：直接赋 `delta_W = np.zeros((d, k))` 而不是 `B @ A`

### 问题3：session_metrics.jsonl 补正确记录
- 文件：`.evolve/memory/session_metrics.jsonl`
- 追加 `{"session_id":"20260418-200229","timestamp":"...","test_count":489,"knowledge_nodes":22,"verdict":"PASS","score":5,"notes":"node22 LoRA"}`

## 验证
1. `cd notebooks && jupyter nbconvert --to notebook --execute 22-lora-2021.ipynb --inplace --ExecutePreprocessor.timeout=120` — 零错误
2. `pytest tests/test_lora.py -v` — 34 tests pass
3. 检查 notebook Cell 5 输出：LoRA 最终 loss 应与全量微调接近（差距 < 0.05）
4. 确认 Cell 3 无 RuntimeWarning 输出
