# Session 20260418-142419 Plan

## 目标
兑现上次承诺：交付节点08 BERT（2018）三件套（doc + notebook + pytest），在同一 session 内完成。

## 方案
1. `docs/08-bert-2018.md` — ~2500字，覆盖：上下文无关词向量缺陷→ELMo/GPT-1局限→BERT双向预训练→MLM机制→NSP机制→输入表示→Fine-tuning范式→局限→衔接RoBERTa/GPT-2
2. `tools/gen_nb_08.py` → `notebooks/08-bert-2018.ipynb` — 7 cells纯NumPy实现：上下文对比、mask_tokens、make_bert_input、BERT嵌入层、encoder forward、分类头、预训练损失
3. `tests/test_bert.py` — 约20条pytest测试，覆盖MLM遮蔽逻辑、输入格式、嵌入形状、encoder输出、分类头
4. `refs/references.bib` — 新增 devlin2018bert（arxiv:1810.04805）、peters2018elmo（arxiv:1802.05365）、radford2018gpt（OpenAI TR）

## 验证
- `python tools/gen_nb_08.py` 无错
- `jupyter nbconvert --to notebook --execute notebooks/08-bert-2018.ipynb` 执行零错误
- `python tools/cite-verify refs/references.bib` 全通过
- `pytest tests/ -q` 新增≥18条测试
