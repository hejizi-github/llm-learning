# Session Plan: 20260418-233151

## 目标
从 CLEAN SLATE 重新引导：建立验证工具链 + 第一个知识节点（Rosenblatt 感知机 1958）

## 方案

### 文件清单
1. `tools/notebook-run` — 批量运行所有 notebooks，检测 kernel errors
2. `tools/cite-verify` — 解析 refs/references.bib，验证每条引用有合法 DOI 或 arxiv ID
3. `tools/md-link-check` — 检查所有 docs/*.md 里的内部链接是否存在
4. `tools/depth-score` — 给每个知识节点打深度分（1-5）
5. `strategies/quality-assessment.md` — 深度评分标准 + 可读性标准
6. `refs/references.bib` — 第一条引用：Rosenblatt 1958（先 fetch 验证 DOI）
7. `refs/citations.jsonl` — 引用溯源记录
8. `docs/01-perceptron-1958.md` — 感知机知识节点文档（面向14岁读者）
9. `notebooks/01-perceptron-1958.ipynb` — 可运行 notebook（从零手撕感知机）

### 工具逻辑
- notebook-run: `jupyter nbconvert --execute --to notebook` 逐个跑，记录失败
- cite-verify: 解析 .bib，检查每条有 doi 或 eprint/url 字段，没有则报 UNVERIFIED
- md-link-check: 提取 `[text](./path)` 形式链接，检查文件是否存在
- depth-score: 给每个 docs/*.md 检查 5 个维度：原始论文引用、代码实现、数学公式、读者导向讲解、历史背景

## 验证
```bash
tools/notebook-run       # 必须 0 failures
tools/cite-verify        # 必须 0 UNVERIFIED
tools/md-link-check      # 必须 0 broken links
tools/depth-score        # node 01 必须 ≥ 3/5
```

## 执行顺序
1. Fetch Rosenblatt 1958 DOI 验证 → 确认引用真实
2. 建 4 个工具
3. 建 refs/
4. 建 docs/01 + notebooks/01
5. 运行验证
6. 提交
