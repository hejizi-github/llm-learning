# 写作策略

状态：活跃  
建立：2026-04-19（session 20260419-021418）

## 核心约束

本知识库的读者是 14 岁学生，有基础代数和简单 Python，没学过微积分。
**读者看不懂 = 任务失败。**

## 写作顺序（每个知识节点必须遵守）

```
1. 开篇钩子          ← 从读者已知的现象开始（不是从定义）
2. 旧方法的失败      ← 为什么需要这个突破？
3. 直觉/类比         ← 用 14 岁学生的日常生活类比
4. 具体代码          ← 可运行的 Python，从零实现
5. 数学符号（可选）  ← 公式是代码的注解，放最后
6. 局限与引出        ← 这个突破又有什么问题？引出下一个节点
```

禁止顺序：定义 → 公式 → 例子

## 类比来源（面向 14 岁）

✅ 允许：考试分数、足球比赛、游戏分数、去不去操场、辨别手写字/猫
❌ 禁止：股市、绩效考核、企业决策、投资组合

## 大师参照

写完任何节点，用 `refs/masters/writing-for-kids.md` 里的 Rubric 自检。
所有原则都来自真实样本（Karpathy、Nielsen），不是训练记忆均值。

## 自检通过标准

写完一段后问：「如果我把这段念给一个 14 岁学生，
他需要先有什么前置知识才能听懂？」
如果前置知识超出了初中代数+基础 Python，就重写。

## Metrics 写入规范（必须遵守）

**Agent 在 session 中只能写 PENDING，不得写自评分数。**

- Agent 调用 `update-metrics.sh` 时：verdict 必须为 `PENDING`，score 必须为 `null` 或 `0`
- 真实的 `review_score` 和 `review_verdict` 只能由外部评审脚本写入
- 违反后果：外部评审会发现 Agent 自评分 ≠ 实际分，浪费两次 session 修补

根源教训（session 062941）：Agent 自评 8.0/PASS → 外部评审 5.0/NEEDS_IMPROVEMENT → P1 被扣 2 分，且两次均犯同一错误。

## 历史教训

- session 20260419-014925：读了 AlexNet PDF，但写出来是"大学教程风格"——
  根因是从论文内容直接写文章，没有先学"怎么给初中生写"
- 两次 clean slate 都是同一个原因
- 修复方案：本文件 + refs/masters/writing-for-kids.md 作为必读参照
